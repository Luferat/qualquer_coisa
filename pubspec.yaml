# pubspec.yaml

name: qualquer_coisa
description: "Um aplicativo experimental com Dart / Flutter / Firebase para a turma de Java CG 2025.1."
publish_to: 'none'

version: 2026.01.30

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true
